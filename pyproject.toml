[project]
name = "bookstore-novo"
version = "0.1.0"
description = ""
authors = [
    {name = "devjosemario",email = "joosemariovm@gmail.com"}
]
requires-python = "^3.10"
dependencies = [
    "django (>=3.2,<4.0)",
    "djangorestframework (>=3.14,<4.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
